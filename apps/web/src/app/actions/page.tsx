import { PreviewState } from '@/components/preview-state';

export default function ActionsPage() {
  return (
    <PreviewState
      description="Track approved priorities by owner, due date, expected signal, and review date."
      eyebrow="Human-owned follow-through"
      nextMilestone="approved action tracking"
      title="Actions"
    />
  );
}
